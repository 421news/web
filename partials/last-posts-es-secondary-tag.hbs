<div class="preview-section">
    <div class="global-container">
        <div class="pv-huge">
            <div class="preview-section_flex-header pb-small">
                {{!-- `title` es opcional y solo lo pasa tag.hbs (la plantilla genérica), para
                      mostrar el nombre lindo del tag en vez del slug: "Magic: The Gathering"
                      y no "magic-the-gathering". Las 20 plantillas dedicadas siguen pasando
                      solo `tag` y caen en el else, sin cambios. --}}
                <h2 style="text-transform: capitalize">{{#if title}}{{title}}{{else}}{{tag}}{{/if}}</h2>
            </div>
            <div class="last-posts-section">
                {{#get "posts"
                    include="tags,authors"
                    filter="tag:{{tag}}+tag:-'hash-en'"
                    limit="15"
                    page="1"
                    order="published_at desc"
                    as |articles|
                }}
                    {{#if articles}}
                        {{#foreach articles}}
                            {{> "post-card"}}
                        {{/foreach}}
                    {{else}}
                        No hay posts.
                    {{/if}}
                {{/get}}
            </div>
            <button class="button" id="load-more" data-next="{{next}}" style="display: block; margin: auto; margin-top: 50px;">
                Cargar más
            </button>
            {{!-- <button
                class="button"
                style="display: block; margin: auto; margin-top: 50px;"
            >
                <a href="/ultimos-posts/" style="color: var(--crema); text-decoration: none;">
                    Ver todas
                </a>
            </button> --}}
        {{!-- <div class="post-cols w-dyn-list">
            {{> "tps_alt-es" tag="Cultura" slug="cultura"}}
            {{> "tps_alt-es" tag="Tecnología" slug="tecnologia"}}
            {{> "tps_alt-es" tag="Juegos" slug="juegos"}}
            {{> "tps_alt-es" tag="Vida real" slug="vida-real"}}
        </div> --}}
        </div>
    </div>
</div>