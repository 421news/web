{{!< default}}
{{> "breadcrumb-tag" tagName="Vida real"}}

  <div class="page-wrapper">
    {{> "general-styling"}}
    {{> "site-nav-es"}}
    <main>
      <!-- TAG HERO (featured + intro) -->
      {{> "tag-hero-section" tagSlug="vida-real" languageFilter="-'hash-en'" eyebrow="Tema" postsLabel="notas"}}

      <!-- LAST POSTS -->
      {{> "last-posts-es-tag" tag="vida-real"}}

    </main>

    <!-- FILE BROWSER NAVIGATION -->
    <div class="global-container pt-small pb-small">
      {{> "file-browser"}}
    </div>
    {{> "site-footer"}}

  </div>
  <script src="{{asset "js/pagination.js"}}" defer></script>
